<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🚨 Срочные вакансии</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 0;
        }

        .header {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(20px);
            padding: 15px 20px;
            position: sticky;
            top: 0;
            z-index: 100;
            border-bottom: 1px solid rgba(255,255,255,0.3);
            box-shadow: 0 2px 20px rgba(0,0,0,0.1);
        }

        .header-title {
            color: #2d3748;
            font-size: 24px;
            font-weight: 800;
            text-align: center;
            margin-bottom: 15px;
            background: linear-gradient(135deg, #667eea, #764ba2);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .filters {
            display: flex;
            gap: 8px;
            overflow-x: auto;
            padding-bottom: 5px;
            scrollbar-width: none;
            -ms-overflow-style: none;
        }

        .filters::-webkit-scrollbar {
            display: none;
        }

        .filter-btn {
            background: rgba(102, 126, 234, 0.2);
            color: #667eea;
            border: none;
            padding: 8px 16px;
            border-radius: 20px;
            font-weight: 600;
            font-size: 14px;
            white-space: nowrap;
            cursor: pointer;
            transition: all 0.3s ease;
            border: 1px solid rgba(102, 126, 234, 0.3);
        }

        .filter-btn.active {
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
            transform: scale(1.05);
            box-shadow: 0 4px 15px rgba(102, 126, 234, 0.3);
        }

        .filter-btn:hover {
            background: rgba(102, 126, 234, 0.3);
            transform: translateY(-1px);
        }

        .jobs-container {
            padding: 20px;
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        .job-card {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 20px;
            padding: 20px;
            cursor: pointer;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
            border: 2px solid transparent;
            backdrop-filter: blur(10px);
        }

        .job-card.high-urgency {
            border-color: #ff6b6b;
            box-shadow: 0 8px 25px rgba(255, 107, 107, 0.3);
        }

        .job-card.medium-urgency {
            border-color: #ffa726;
            box-shadow: 0 8px 25px rgba(255, 167, 38, 0.3);
        }

        .job-card.low-urgency {
            border-color: #42a5f5;
            box-shadow: 0 8px 25px rgba(66, 165, 245, 0.3);
        }

        .job-card:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 35px rgba(0,0,0,0.2);
        }

        .urgency-badge {
            position: absolute;
            top: 12px;
            right: 12px;
            padding: 6px 12px;
            border-radius: 15px;
            font-size: 12px;
            font-weight: 700;
            text-transform: uppercase;
        }

        .urgency-high {
            background: linear-gradient(135deg, #ff6b6b, #ee5a24);
            color: white;
            animation: urgentPulse 1.5s infinite;
        }

        .urgency-medium {
            background: linear-gradient(135deg, #ffa726, #ff7043);
            color: white;
        }

        .urgency-low {
            background: linear-gradient(135deg, #42a5f5, #1e88e5);
            color: white;
        }

        @keyframes urgentPulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.05); }
        }

        .job-header {
            display: flex;
            align-items: center;
            margin-bottom: 12px;
        }

        .job-icon {
            width: 45px;
            height: 45px;
            border-radius: 12px;
            background: linear-gradient(135deg, #667eea, #764ba2);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 20px;
            margin-right: 15px;
            color: white;
        }

        .job-info {
            flex: 1;
        }

        .job-title {
            font-size: 18px;
            font-weight: 700;
            color: #2d3748;
            margin-bottom: 4px;
        }

        .job-location {
            font-size: 14px;
            color: #718096;
            font-weight: 500;
        }

        .job-schedule {
            margin-bottom: 12px;
        }

        .job-date {
            font-size: 14px;
            font-weight: 700;
            color: #2d3748;
            margin-bottom: 4px;
        }

        .job-time {
            background: rgba(102, 126, 234, 0.1);
            color: #667eea;
            padding: 6px 12px;
            border-radius: 12px;
            font-size: 13px;
            font-weight: 600;
            display: inline-block;
        }

        .job-location-full {
            font-size: 13px;
            color: #718096;
            margin: 8px 0;
            display: flex;
            align-items: center;
            gap: 6px;
        }

        .job-pay-section {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 15px;
            background: rgba(46, 213, 115, 0.1);
            padding: 10px 15px;
            border-radius: 12px;
        }

        .job-pay {
            font-size: 16px;
            font-weight: 800;
            color: #2ed573;
        }

        .job-pay-period {
            font-size: 12px;
            color: #718096;
            font-weight: 600;
        }

        .job-tags {
            display: flex;
            gap: 8px;
            flex-wrap: wrap;
        }

        .job-tag {
            background: rgba(102, 126, 234, 0.1);
            color: #667eea;
            padding: 4px 10px;
            border-radius: 10px;
            font-size: 12px;
            font-weight: 600;
        }

        .urgent-indicator {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 3px;
            background: linear-gradient(90deg, #ff6b6b, #ffa726, #42a5f5);
            background-size: 200% 100%;
            animation: urgentGradient 2s ease-in-out infinite;
        }

        @keyframes urgentGradient {
            0%, 100% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
        }

        .floating-counter {
            position: fixed;
            bottom: 20px;
            right: 20px;
            background: linear-gradient(135deg, #ff6b6b, #ee5a24);
            color: white;
            padding: 12px 18px;
            border-radius: 25px;
            font-weight: 700;
            box-shadow: 0 10px 30px rgba(255, 107, 107, 0.4);
            animation: counterFloat 3s ease-in-out infinite;
            z-index: 50;
        }

        @keyframes counterFloat {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-5px); }
        }

        .job-card.hidden {
            display: none;
        }

        .no-results {
            text-align: center;
            color: rgba(255,255,255,0.8);
            font-size: 18px;
            font-weight: 600;
            margin-top: 40px;
        }

        /* Модальное окно */
        .modal-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0,0,0,0.8);
            z-index: 1000;
            display: none;
            backdrop-filter: blur(5px);
        }

        .modal-content {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: white;
            overflow-y: auto;
            transform: translateY(100%);
            transition: transform 0.4s ease;
        }

        .modal-overlay.active .modal-content {
            transform: translateY(0);
        }

        .modal-header {
            background: linear-gradient(135deg, #ff6b6b, #ee5a24);
            padding: 20px;
            color: white;
            position: relative;
        }

        .close-btn {
            position: absolute;
            top: 20px;
            right: 20px;
            background: rgba(255,255,255,0.2);
            border: none;
            color: white;
            width: 40px;
            height: 40px;
            border-radius: 50%;
            font-size: 20px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .modal-body {
            padding: 25px;
        }

        .detail-section {
            margin-bottom: 25px;
            background: rgba(102, 126, 234, 0.05);
            padding: 20px;
            border-radius: 15px;
        }

        .detail-title {
            font-size: 18px;
            font-weight: 700;
            color: #2d3748;
            margin-bottom: 15px;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .detail-item {
            display: flex;
            align-items: center;
            margin-bottom: 12px;
            padding: 12px;
            background: white;
            border-radius: 10px;
            border-left: 4px solid #667eea;
        }

        .detail-icon {
            margin-right: 12px;
            font-size: 18px;
        }

        .apply-section {
            position: sticky;
            bottom: 0;
            background: white;
            padding: 20px;
            border-top: 1px solid rgba(0,0,0,0.1);
            display: flex;
            gap: 12px;
        }

        .apply-btn {
            flex: 2;
            background: linear-gradient(135deg, #ff6b6b, #ee5a24);
            color: white;
            border: none;
            padding: 18px;
            border-radius: 15px;
            font-weight: 800;
            font-size: 16px;
            cursor: pointer;
            text-transform: uppercase;
        }

        .call-btn {
            flex: 1;
            background: linear-gradient(135deg, #2ed573, #17c0eb);
            color: white;
            border: none;
            padding: 18px;
            border-radius: 15px;
            font-weight: 700;
            font-size: 20px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s ease;
        }

        .call-btn:hover {
            transform: scale(1.05);
        }
    </style>
</head>
<body>
    <div class="header">
        <h1 class="header-title">🚨 Срочные вакансии</h1>
        <div class="filters">
            <button class="filter-btn active" data-filter="all">Все</button>
            <button class="filter-btn" data-filter="бармен">🍸 Бармен</button>
            <button class="filter-btn" data-filter="официант">🍽️ Официант</button>
            <button class="filter-btn" data-filter="повар">👨‍🍳 Повар</button>
            <button class="filter-btn" data-filter="хостес">💁‍♀️ Хостес</button>
            <button class="filter-btn" data-filter="бариста">☕ Бариста</button>
            <button class="filter-btn" data-filter="кассир">💳 Кассир</button>
        </div>
    </div>

    <div class="jobs-container" id="jobsContainer">
        <!-- Срочные вакансии будут добавлены через JavaScript -->
    </div>

    <div class="floating-counter" id="urgentCounter">
        🔥 5 срочных
    </div>

    <!-- Модальное окно -->
    <div class="modal-overlay" id="modalOverlay">
        <div class="modal-content">
            <div class="modal-header">
                <button class="close-btn" onclick="closeModal()">×</button>
                <h2 id="modalTitle">Подробности вакансии</h2>
            </div>
            <div class="modal-body" id="modalBody">
                <!-- Подробная информация будет загружена сюда -->
            </div>
            <div class="apply-section">
                <button class="apply-btn" onclick="applyToJob()">🚨 Откликнуться на вакансию</button>
            </div>
        </div>
    </div>

    <script>
        // Данные срочных вакансий
        const urgentJobs = [
            {
                id: 1,
                title: "Бармен",
                location: "Ресторан Шерлок",
                date: "Завтра, 1 сентября",
                timeStart: "18:00",
                timeEnd: "02:00",
                address: "ул. Жибек Жолы, 135",
                pay: "15 000 ₸",
                payPeriod: "за смену",
                urgency: "high",
                category: "бармен",
                icon: "🍸",
                reason: "Коллега заболел",
                phone: "+7 701 234 5678",
                details: {
                    shift: "18:00 - 02:00 (8 часов)",
                    requirements: ["Классические коктейли", "Знание алкоголя", "Навыки бариста", "Стрессоустойчивость"],
                    benefits: ["Питание", "Такси домой", "Быстрое оформление", "Возможность постоянной работы"],
                    fullAddress: "Алматы, ул. Жибек Жолы, 135"
                }
            },
            {
                id: 2,
                title: "Официант",
                location: "Кафе Central",
                date: "Сегодня, 31 августа",
                timeStart: "14:00",
                timeEnd: "22:00",
                address: "пр. Назарбаева, 65",
                pay: "12 000 ₸",
                payPeriod: "за смену",
                urgency: "high",
                category: "официант",
                icon: "🍽️",
                reason: "Не вышел на смену",
                phone: "+7 705 987 6543",
                details: {
                    shift: "14:00 - 22:00 (8 часов)",
                    requirements: ["Опыт работы", "Знание меню", "Вежливость", "Быстрота"],
                    benefits: ["Питание", "Чаевые", "Гибкий график"],
                    fullAddress: "Алматы, пр. Назарбаева, 65"
                }
            },
            {
                id: 3,
                title: "Повар",
                location: "Быстрое Питание",
                date: "Завтра, 1 сентября",
                timeStart: "08:00",
                timeEnd: "20:00",
                address: "ул. Сатпаева, 90",
                pay: "18 000 ₸",
                payPeriod: "за смену",
                urgency: "medium",
                category: "повар",
                icon: "👨‍🍳",
                reason: "Срочная замена",
                phone: "+7 777 123 4567",
                details: {
                    shift: "08:00 - 20:00 (12 часов)",
                    requirements: ["Опыт на кухне", "Знание фаст-фуда", "Санитарная книжка"],
                    benefits: ["Питание", "Обучение", "Премии"],
                    fullAddress: "Алматы, ул. Сатпаева, 90"
                }
            },
            {
                id: 4,
                title: "Хостес",
                location: "Lounge Bar Sky",
                date: "Сегодня, 31 августа",
                timeStart: "20:00",
                timeEnd: "03:00",
                address: "ул. Панфилова, 120",
                pay: "10 000 ₸",
                payPeriod: "за смену",
                urgency: "high",
                category: "хостес",
                icon: "💁‍♀️",
                reason: "Неожиданная нехватка",
                phone: "+7 708 555 7890",
                details: {
                    shift: "20:00 - 03:00 (7 часов)",
                    requirements: ["Приятная внешность", "Коммуникабельность", "Опыт в HoReCa"],
                    benefits: ["Чаевые", "Такси", "Красивая форма"],
                    fullAddress: "Алматы, ул. Панфилова, 120"
                }
            },
            {
                id: 5,
                title: "Бариста",
                location: "Coffee House",
                date: "Завтра, 1 сентября",
                timeStart: "07:00",
                timeEnd: "15:00",
                address: "ул. Абая, 45",
                pay: "13 000 ₸",
                payPeriod: "за смену",
                urgency: "medium",
                category: "бариста",
                icon: "☕",
                reason: "Отпуск коллеги",
                phone: "+7 747 888 9999",
                details: {
                    shift: "07:00 - 15:00 (8 часов)",
                    requirements: ["Знание кофейных напитков", "Латте-арт", "Работа с кофемашиной"],
                    benefits: ["Бесплатный кофе", "Обучение", "Дружная команда"],
                    fullAddress: "Алматы, ул. Абая, 45"
                }
            },
            {
                id: 6,
                title: "Кассир",
                location: "Фуд-корт Mega",
                date: "Сегодня, 31 августа",
                timeStart: "10:00",
                timeEnd: "18:00",
                address: "пр. Розыбакиева, 247",
                pay: "8 000 ₸",
                payPeriod: "за смену",
                urgency: "low",
                category: "кассир",
                icon: "💳",
                reason: "Дополнительная смена",
                phone: "+7 702 111 2233",
                details: {
                    shift: "10:00 - 18:00 (8 часов)",
                    requirements: ["Работа с кассой", "Внимательность", "Вежливость"],
                    benefits: ["Питание", "Скидки", "Удобный график"],
                    fullAddress: "Алматы, пр. Розыбакиева, 247"
                }
            }
        ];

        function createJobCard(job) {
            const urgencyClass = `urgency-${job.urgency}`;
            const cardClass = `${job.urgency}-urgency`;
            
            return `
                <div class="job-card ${cardClass}" data-category="${job.category}" onclick="openModal(${job.id})">
                    <div class="urgent-indicator"></div>
                    <div class="urgency-badge ${urgencyClass}">
                        ${job.urgency === 'high' ? '🚨 СРОЧНО' : job.urgency === 'medium' ? '⚡ НАДО' : '📅 ПОДХОДИТ'}
                    </div>
                    
                    <div class="job-header">
                        <div class="job-icon">${job.icon}</div>
                        <div class="job-info">
                            <div class="job-title">${job.title}</div>
                            <div class="job-location">${job.location}</div>
                        </div>
                    </div>

                    <div class="job-schedule">
                        <div class="job-date">📅 ${job.date}</div>
                        <div class="job-time">⏰ ${job.timeStart} - ${job.timeEnd}</div>
                    </div>

                    <div class="job-location-full">
                        <span>📍</span> ${job.address}
                    </div>
                    
                    <div class="job-pay-section">
                        <div>
                            <div class="job-pay">${job.pay}</div>
                            <div class="job-pay-period">${job.payPeriod}</div>
                        </div>
                    </div>
                    
                    <div class="job-tags">
                        <div class="job-tag">${job.reason}</div>
                    </div>
                </div>
            `;
        }

        function renderJobs() {
            const container = document.getElementById('jobsContainer');
            container.innerHTML = urgentJobs.map(createJobCard).join('');
            updateCounter();
        }

        function filterJobs(category) {
            const cards = document.querySelectorAll('.job-card');
            let visibleCount = 0;
            
            cards.forEach(card => {
                if (category === 'all' || card.dataset.category === category) {
                    card.classList.remove('hidden');
                    visibleCount++;
                } else {
                    card.classList.add('hidden');
                }
            });

            // Обновляем активную кнопку фильтра
            document.querySelectorAll('.filter-btn').forEach(btn => {
                btn.classList.remove('active');
                if (btn.dataset.filter === category) {
                    btn.classList.add('active');
                }
            });

            // Показываем сообщение если нет результатов
            const container = document.getElementById('jobsContainer');
            const existingNoResults = container.querySelector('.no-results');
            if (existingNoResults) {
                existingNoResults.remove();
            }

            if (visibleCount === 0) {
                const noResults = document.createElement('div');
                noResults.className = 'no-results';
                noResults.innerHTML = '🔍 Нет срочных вакансий в этой категории';
                container.appendChild(noResults);
            }

            updateCounter(visibleCount);
        }

        function updateCounter(count = null) {
            const counter = document.getElementById('urgentCounter');
            const visibleJobs = count !== null ? count : document.querySelectorAll('.job-card:not(.hidden)').length;
            counter.textContent = `🔥 ${visibleJobs} срочных`;
        }

        function openModal(jobId) {
            const job = urgentJobs.find(j => j.id === jobId);
            const modal = document.getElementById('modalOverlay');
            const modalTitle = document.getElementById('modalTitle');
            const modalBody = document.getElementById('modalBody');

            modalTitle.textContent = `${job.icon} ${job.title} - ${job.location}`;
            
            modalBody.innerHTML = `
                <div class="detail-section">
                    <div class="detail-title">⏰ Детали смены</div>
                    <div class="detail-item">
                        <span class="detail-icon">📅</span>
                        <div><strong>Время:</strong> ${job.details.shift}</div>
                    </div>
                    <div class="detail-item">
                        <span class="detail-icon">📍</span>
                        <div><strong>Адрес:</strong> ${job.details.address}</div>
                    </div>
                    <div class="detail-item">
                        <span class="detail-icon">💰</span>
                        <div><strong>Оплата:</strong> ${job.pay} + чаевые</div>
                    </div>
                </div>

                <div class="detail-section">
                    <div class="detail-title">📋 Требования</div>
                    ${job.details.requirements.map(req => `
                        <div class="detail-item">
                            <span class="detail-icon">✅</span>
                            <div>${req}</div>
                        </div>
                    `).join('')}
                </div>

                <div class="detail-section">
                    <div class="detail-title">🎁 Что предлагаем</div>
                    ${job.details.benefits.map(benefit => `
                        <div class="detail-item">
                            <span class="detail-icon">⭐</span>
                            <div>${benefit}</div>
                        </div>
                    `).join('')}
                </div>
            `;

            modal.style.display = 'block';
            setTimeout(() => modal.classList.add('active'), 10);
        }

        function closeModal() {
            const modal = document.getElementById('modalOverlay');
            modal.classList.remove('active');
            setTimeout(() => modal.style.display = 'none', 400);
        }

        function applyToJob() {
            closeModal();
            showNotification('✅ Отклик отправлен! Менеджер свяжется с вами в течение 10 минут.');
        }

        function showNotification(message) {
            const notification = document.createElement('div');
            notification.style.cssText = `
                position: fixed;
                top: 20px;
                left: 50%;
                transform: translateX(-50%);
                background: linear-gradient(135deg, #2ed573, #17c0eb);
                color: white;
                padding: 15px 20px;
                border-radius: 15px;
                font-weight: 700;
                box-shadow: 0 10px 30px rgba(46, 213, 115, 0.4);
                z-index: 2000;
                animation: slideDown 0.5s ease;
                max-width: 90%;
                text-align: center;
            `;
            notification.textContent = message;
            
            document.body.appendChild(notification);
            
            setTimeout(() => {
                notification.style.animation = 'slideDown 0.5s ease reverse';
                setTimeout(() => notification.remove(), 500);
            }, 3000);
        }

        // Инициализация
        document.addEventListener('DOMContentLoaded', function() {
            renderJobs();

            // Добавляем обработчики фильтров
            document.querySelectorAll('.filter-btn').forEach(btn => {
                btn.addEventListener('click', () => {
                    filterJobs(btn.dataset.filter);
                });
            });

            // Закрытие модального окна по клику на overlay
            document.getElementById('modalOverlay').addEventListener('click', function(e) {
                if (e.target === this) {
                    closeModal();
                }
            });
        });

        // Добавляем стили для анимации уведомлений
        const style = document.createElement('style');
        style.textContent = `
            @keyframes slideDown {
                from { transform: translateX(-50%) translateY(-100%); opacity: 0; }
                to { transform: translateX(-50%) translateY(0); opacity: 1; }
            }
        `;
        document.head.appendChild(style);
    </script>
</body>
</html>