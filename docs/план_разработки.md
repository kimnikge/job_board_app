# 🚀 НОВЫЙ ПЛАН РАЗВИТИЯ ПРОЕКТА (v2)

Документ описывает следующую волну развития после завершения базового рефакторинга архитектуры. Цель — перейти от MVP к дифференцированному продукту с интерактивным профилем кандидата (видео, бейджи, прогресс навыков), геймификацией и подготовкой к PWA + Telegram WebApp.

---

## 1. Вижен / Ценность
Создать платформу для общепита Астаны, где профиль кандидата — живое портфолио (короткое видео + визуальные навыки + достижения), а срочные вакансии и быстрый найм обеспечивают высокую скорость закрытия смен.

Ключевые отличия:
- Видео-приветствие (до 15 сек) + динамическое превью
- Система бейджей (авто + ручная выдача) с влиянием на навыки
- Шкалы навыков 0–100 с пересчётом от бейджей
- Таймлайн опыта (смены / часы / заведения)
- PWA + Telegram WebApp доступ

---

## 2. Диапазон ближайших релизов
| Релиз | Основной фокус | Результат |
|-------|----------------|-----------|
| R1 | Data Model Expansion | Схема БД + сервисы + заглушки UI |
| R2 | Interactive Profile Core | Видео, навыки, бейджи (MVP) |
| R3 | Logic & Automation | Триггеры, авто-выдача бейджей, пересчёт skills |
| R4 | UX & Visual Upgrade | Tailwind, адаптивные компоненты, карусели |
| R5 | PWA & Offline | Manifest, SW, кеши |
| R6 | Telegram WebApp | Авторизация + адаптация UI |
| R7 | Sharing & Recommendations | Deep links, рекомендации, share |
| R8 | Hardening | Тесты, оптимизация, безопасность |

---

## 3. Текущее состояние (резюме)
Завершён базовый переход к упрощённой структуре (роутинг, stores, сервисы). Профиль текстовый, отсутствуют ключевые сущности (skills, badges, work_logs, video). Локальные JSON используются для демонстрации.

---

## 4. Архитектурные принципы (актуализировано)
1. Single Source of Truth — Supabase (демо-режим только через адаптер demoData)
2. Fat Backend / Thin Front Logic для вычислимых полей (уровни навыков, авто бейджи)
3. Идемпотентные миграции (baseline + инкременты, без форс-reset)
4. Контракт сервисов стабилен до изменения схемы (версионирование при необходимости)
5. Ленивая загрузка тяжёлых секций (badges, timeline)

---

## 5. Новая схема данных (высокий уровень)
- user_profiles: full_name, short_bio(<=100), primary_role, video_url, avatar_url, experience_years
- skills: id, user_id, name, category, base_level, calculated_level, updated_at
- badges: id, user_id, employer_id, name, icon_url, description, source(auto|manual), awarded_at
- badge_skill_links: badge_id, skill_name, delta
- work_logs: id, user_id, employer_id, period_from, period_to, shifts_count, total_hours
- employers: id, name, logo_url, district_id

Триггеры:
- on insert work_logs → check thresholds → award badge
- on insert badges → recalc user skills

Функции (RPC):
- recalc_skills(user_id)
- get_user_profile_full(user_id) (агрегирует badges, skills, work_logs)

---

## 6. Подробный план по фазам

### Фаза 0 (DONE) — Базовая архитектура
Роутинг, stores, упрощённые сервисы, тесты (25+). ✅

### Фаза 1 — Data Model Expansion (R1)
- [ ] ERD финализация + диаграмма
- [ ] Consolidated baseline migration (новые таблицы)
- [ ] Политики RLS (owner access)
- [ ] Storage bucket: profile_videos
- [ ] Индексы: skills(user_id), badges(user_id, awarded_at), work_logs(user_id, employer_id)
- [ ] Сервисы: skills.service.js, badges.service.js, experience.service.js, media.service.js (скелет)
- Output: схема готова, сервисы отдают mock/пустые массивы

### Фаза 2 — Interactive Profile UI (R2)
- [ ] Компонент VideoProfile (функционал upload/play) — SCAFFOLD ГОТОВ в R1, реализация (логика/сторидж) откладывается до активного старта R2
- [ ] Компонент SkillBar (цветовые диапазоны)
- [ ] Компонент BadgeCarousel (гор. скролл)
- [ ] Компонент ExperienceTimeline (карточки)
- [ ] Расширение profile store (badges[], skills[], experience[], videoUrl) — ГОТОВО (структура); динамика видео активируется при включении feature flag
- [ ] Ленивая загрузка секций (import())
- Note: FEATURE_FLAGS.videoProfile=false до готовности UX и ограничений размера.
- Output: профиль визуально интерактивный c mock данными

### Фаза 3 — Logic & Automation (R3)
- [ ] Триггер auto badge по shifts_count / total_hours
- [ ] Функция recalc_skills + тест вручную (SQL)
- [ ] RPC get_user_profile_full
- [ ] Интеграция сервисов с реальными данными Supabase
- [ ] Обновлённые тесты сервисов (Vitest mocks → live)
- Output: данные динамически отражают бейджи/навыки

### Фаза 4 — Visual & Tailwind (R4)
- [ ] Подключение Tailwind (postcss + config)
- [ ] Tokens: цвета, spacing, shadows
- [ ] Перенос новых компонентов на utility классы
- [ ] Адаптив (sm / md / lg) для профиля
- [ ] Skeleton loaders для секций
- Output: визуально унифицированный UI

### Фаза 5 — PWA (R5)
- [ ] manifest.json + иконки
- [ ] Service Worker (workbox или custom) — cache static + API fallback
- [ ] Lighthouse PWA score ≥ 85
- [ ] Offline профиль (кеш последнего snapshot)
- Output: установка на устройство и offline view

### Фаза 6 — Telegram WebApp (R6)
- [ ] Базовая авторизация через Telegram init data
- [ ] Верификация подписи на бэкенде (Edge Function)
- [ ] Адаптация высоты / цвета темы
- [ ] Ограниченный режим навигации (fullscreen profile / jobs)
- Output: работает внутри Telegram клиента

### Фаза 7 — Sharing & Recommendations (R7)
- [ ] Deep link /profile/:id public view
- [ ] RecommendationBadge (employer → candidate)
- [ ] Share кнопка (Web Share API + fallback copy)
- [ ] Генерация оглавления Open Graph мета
- Output: внешний просмотр профиля + доверительные сигналы

### Фаза 8 — Hardening & QA (R8)
- [ ] Security review RLS / политика выдачи бейджей
- [ ] Нагрузочный тест: массовая выдача work_logs
- [ ] Оптимизация SQL (EXPLAIN план на recalc)
- [ ] Финальные тесты (добавить ≥10 новых)
- [ ] Документация API (таблицы + RPC)
- Output: готовность к публичному пилоту

---

## 7. Acceptance / Definition of Done по ключевым блокам
| Блок | DoD |
|------|-----|
| Skills | CRUD + пересчёт + прогресс-бары отображают calculated_level |
| Badges | Присвоение manual + auto, отображение в карусели, tooltip описания |
| Video | Upload < 25MB, превью работает на mobile/desktop, fallback аватар |
| Timeline | Сортировка по дате, бейджи внутри периода, сумма часов |
| Profile API | Один RPC отдаёт агрегированную структуру < 400ms |
| PWA | Lighthouse PWA ≥ 85 / Performance ≥ 80 |
| Telegram | Авторизация и запуск без ошибок, профиль грузится < 2s |

---

## 8. Метрики успеха (MVP+)
- Время рендера полной страницы профиля (cold) < 1.5s (desktop)
- Среднее кол-во запросов профиля ≤ 4 (RPC агрегирует)
- Размер initial JS bundle ≤ 300KB gzipped (lazy profile extras)
- ≥ 90% test pass rate; покрытие критических сервисов ≥ 70% lines
- PWA Install prompt доступен и успешен (manual QA)

---

## 9. Риск матрица (обновл.)
| Риск | Вероятн. | Влияние | Митигация |
|------|----------|---------|-----------|
| Задержка видео (FFmpeg/превью) | Средняя | Среднее | MVP без генерации — только upload + poster кадр через <video> capture |
| Сложность авто бейджей | Низкая | Среднее | Начать с 1 правила (5 смен) → расширять |
| RLS ошибки (утечка данных) | Низкая | Высокое | Ревью политик + тест кейсы негативные |
| Telegram init data spoof | Средняя | Высокое | Подпись + проверка через hash алгоритм Telegram |
| Рост bundle | Средняя | Среднее | Code-splitting + dynamic import секций профиля |

---

## 10. Немедленный Action List (старт R1)
- ✅ Подготовить ERD (добавить в docs/ERD_v2.png) — текст/DBML готово (ERD_v2.md, ERD_v2.dbml); PNG pending
- ✅ Написать consolidated migration (SQL) + применить
- ✅ Создать RLS политики для новых таблиц (baseline + инкремент user_profiles)
- [ ] Создать bucket profile_videos + политики
- ✅ Добавить skeleton сервисов (skills/badges/experience/media)
- ✅ Расширить profile store под новые поля
- ✅ Создать пустые компоненты (VideoProfile / SkillBar / BadgeCarousel / ExperienceTimeline)
- ✅ Добавить тесты-заглушки (pending) для новых сервисов + тест миграции

---

## 11. Исторические достижения (закрепляем)
Старые этапы (рефакторинг, тесты, упрощение роутинга) считаем завершёнными и не трогаем без необходимости. Этот раздел служит точкой отсечения для новой дорожной карты.

---

## 12. Обновление документа
Документ обновляется после завершения каждой фазы: добавляется таблица статуса и фактические метрики.

Текущий прогресс R1: 7/9 (осталось: bucket profile_videos, ERD_v2.png). Видео: компонент и сервис-заглушка готовы (deferred enable в R2).

---

## 13. Краткое резюме
Мы переходим от «текстового профиля + вакансии» к «интерактивному геймифицированному профилю» с чёткой последовательностью внедрения: сначала схема и сервисы → UI-оболочка → логика навыков/бейджей → визуальная унификация → PWA → Telegram → расширения и шлифовка.

Фокус: не распыляться; каждая фаза закрывает слой. Следующий шаг: выполнить Action List R1.

---

Готово к исполнению.

---

## 14. Детализированная ERD (таблицы v2)
| Таблица | Поле | Тип | Обязат. | По умолчанию | Описание |
|---------|------|-----|---------|--------------|----------|
| user_profiles | id (PK) | uuid | ✔ | gen_random_uuid() | Пользователь = auth.users.id (дублируем для гибкости) |
|  | auth_user_id (FK) | uuid | ✔ |  | Ссылка на auth.users (1:1) |
|  | full_name | text | ✔ |  | Имя |
|  | primary_role | text | ✔ |  | Ключевая роль (chef, waiter...) |
|  | short_bio | text | ✖ |  | <=100 символов |
|  | avatar_url | text | ✖ |  | Путь в storage |
|  | video_url | text | ✖ |  | Путь к видео приветствия |
|  | experience_years | smallint | ✖ | 0 | Полные годы опыта |
|  | created_at | timestamptz | ✔ | now() | Создано |
|  | updated_at | timestamptz | ✔ | now() | Обновлено (триггер) |
| skills | id (PK) | uuid | ✔ | gen_random_uuid() | |
|  | user_id (FK) | uuid | ✔ |  | -> user_profiles.id |
|  | name | text | ✔ |  | Уникально в пределах user_id |
|  | category | text | ✖ |  | kitchen/service/... |
|  | base_level | smallint | ✔ | 0 | 0–100 ввод перен. |
|  | calculated_level | smallint | ✔ | 0 | base + дельты бейджей |
|  | updated_at | timestamptz | ✔ | now() | |
| badges | id (PK) | uuid | ✔ | gen_random_uuid() | |
|  | user_id (FK) | uuid | ✔ |  | -> user_profiles.id |
|  | employer_id (FK) | uuid | ✖ |  | -> employers.id |
|  | name | text | ✔ |  | Отображаемое имя |
|  | icon_url | text | ✖ |  | Путь к иконке |
|  | description | text | ✖ |  | Tooltip |
|  | source | text | ✔ | 'manual' | manual|auto |
|  | awarded_at | timestamptz | ✔ | now() | |
| badge_skill_links | badge_id (FK) | uuid | ✔ |  | -> badges.id |
|  | skill_name | text | ✔ |  | Имя навыка (FK по (user_id, name) логически) |
|  | delta | smallint | ✔ | 0 | + / - изменение |
| work_logs | id (PK) | uuid | ✔ | gen_random_uuid() | |
|  | user_id (FK) | uuid | ✔ |  | -> user_profiles.id |
|  | employer_id (FK) | uuid | ✔ |  | -> employers.id |
|  | period_from | date | ✔ |  | Начало периода |
|  | period_to | date | ✔ |  | Конец периода |
|  | shifts_count | smallint | ✔ | 0 | Кол-во смен |
|  | total_hours | smallint | ✔ | 0 | Часы суммарно |
| employers | id (PK) | uuid | ✔ | gen_random_uuid() | |
|  | name | text | ✔ |  | Название заведения |
|  | logo_url | text | ✖ |  | Логотип |
|  | district_id | smallint | ✖ |  | Справочник районов |

Уникальные / Индексы:
- skills: UNIQUE (user_id, name)
- badges: INDEX (user_id, awarded_at DESC)
- work_logs: INDEX (user_id, employer_id)

## 15. Naming & миграции
- Формат файла: YYYYMMDDHHMMSS_<snake_case>.sql
- Baseline: 20250810xxxxxx_baseline_profile_extension.sql (объединяет новые таблицы)
- Дальше инкременты: add_triggers, add_rpcs, add_indexes
- В миграциях не использовать DROP CASCADE без острой необходимости

## 16. RLS (черновик политик)
1. ENABLE RLS ON ALL new tables
2. user_profiles: SELECT/UPDATE/DELETE WHERE auth.uid() = auth_user_id
3. skills/badges/work_logs: row.user_id = (SELECT id FROM user_profiles WHERE auth_user_id = auth.uid())
4. employers: публичный SELECT (позже ограничить по роли)
5. Функции recalc_skills и award_badge SECURITY DEFINER (владение сервис ролью)

## 17. Контракты сервисов (интерфейс)
skills.service:
- list(userId) -> Skill[]
- upsert(userId, {name, baseLevel}) -> Skill
- recalc(userId) -> Skill[]

badges.service:
- list(userId)
- awardManual(userId, payload)
- listWithSkills(userId)

experience.service (work_logs):
- list(userId)
- addEntry(userId, entry)
- aggregate(userId) -> { totalHours, totalShifts }

media.service:
- uploadProfileVideo(userId, file) -> { url }
- removeVideo(userId)

profile.service (расширение):
- getFull(userId) -> { profile, skills, badges, workLogs }

## 18. Тестовая стратегия
| Фаза | Тесты | Инструмент |
|------|-------|------------|
| R1 | SQL миграции (idempotent), mock сервисы | psql + Vitest |
| R2 | Компоненты UI (snapshot + интеракция) | Vitest + Testing Library |
| R3 | Триггеры / RPC (позитив/негатив) | SQL scripts + Vitest integration |
| R4 | Визуальные регрессии (критичные) | Percy (опционально позже) |
| R5 | PWA Lighthouse | lighthouse ci |
| R6 | Telegram init validation | Vitest + подписанные payload fixtures |
| R7 | Sharing links (OG мета) | Playwright (опц.) |
| R8 | Нагрузочные (work_logs bulk) | k6 / simple node script |

## 19. Observability & Логи
- Front: единый logger util (info,error,perf) -> console + future endpoint
- Backend (SQL): таблица audit_badges (badge_id, user_id, rule, created_at)
- Метрика recalc_skills длительность (ms) (лог в отдельную таблицу performance_events)

## 20. Performance Budget
- Full Profile RPC ≤ 400ms (P95)
- Recalc skills ≤ 120ms при ≤ 50 skills
- Upload video start->URL ≤ 4s (wifi)
- SkillBar интеракция рендер ≤ 16ms кадр

## 21. Rollback стратегия
1. Каждая миграция имеет down-блок (если возможно)
2. При ошибке на prod: disable triggers → revert last migration → re-enable
3. Backup до применения набора миграций (Supabase dump)

## 22. Переход с demo JSON на БД
R1: Параллельная загрузка (если нет данных в Supabase — fallback JSON)
R2: Завершить перенос: убрать JSON, оставить feature flag ?useDemoData=true
R3: Удалить демо адаптер

## 23. Поэтапное внедрение TypeScript
- Шаг 1 (R2): Добавить tsconfig, типы для новых сервисов (.d.ts)
- Шаг 2 (R3): Перевести сервисы на .ts
- Шаг 3 (R4-R5): Перевести store + ключевые компоненты
- Мини goal: >60% строк TS к R6

## 24. Accessibility / UX базовые стандарты
- Контраст ≥ 4.5:1 для текста
- Keyboard focus: все интерактивные элементы
- ARIA labels для иконок бейджей
- Video: кнопка mute + fallback caption “Видео недоступно”

## 25. Security Checklist
- RLS включён: все таблицы ✔
- Storage bucket: публичное чтение видео? (MVP: auth gated URL)
- Max video size проверка client + server
- Rate limit вручную (квоты на awardManual) — edge function (позже)

## 26. Release cadence & governance
- Каждая фаза = minor релиз (v0.X)
- Hotfix → patch (v0.X.Y)
- Changelog в docs/CHANGELOG.md (добавить при первом релизе R1)

## 27. Glossary
- Skill: измеряемый показатель 0–100
- Badge: достижение, может модифицировать skill
- Delta: изменение base_level через badge
- Work Log: агрегированная запись периода (не по каждой смене)
- RPC: хранимая функция, вызываемая через Supabase JS

## 28. Обновлённый расширенный Action List (R1 детализация)
Infra/Data:
- ✅ Создать baseline миграцию (все таблицы + индексы)
- ✅ Добавить триггер updated_at на user_profiles, skills
- ✅ Создать функции: recalc_skills (пустая), get_user_profile_full (возвращает только profile пока)
- [ ] Создать storage bucket profile_videos (private)

Services / Front:
- ✅ scaffolding: skills.service.js (list mock), badges.service.js, experience.service.js, media.service.js
- ✅ Расширить profile.store.js: поля skills[], badges[], experience[], videoUrl
- ✅ Feature flag demoData (в utils/constants)

Tests:
- ✅ Vitest: добавить pending тесты (describe.skip) для новых сервисов
- ✅ SQL миграция «схема создалась»: select count(*) из information_schema.tables

Docs / Ops:
- [ ] ERD_v2.png (генератор: dbdiagram или draw.io)
- ✅ CHANGELOG.md и первая запись R1 (Unreleased)

Готовность R1: все сервисы возвращают структурно корректные (пусть пустые) массивы; профиль страница не ломается при пустых данных; baseline миграция успешно применена 2 раза подряд в тестовой БД (idempotent).

---

## 29. Финальное расширенное резюме
Добавлены недостающие операционные блоки (миграции, безопасность, тестовая стратегия, TS внедрение, производительность, observability). План теперь покрывает полный жизненный цикл: от схемы и сервисов до релизного процесса и метрик. Следующий практический шаг — выполнить расширенный Action List R1 и зафиксировать ERD.

END.
